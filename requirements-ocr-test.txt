# OCR 測試專用依賴清單
# 這個檔案只包含測試 PaddleOCR 功能所需的最小依賴集

# ============================================
# 核心 OCR 依賴
# ============================================
# PaddlePaddle 深度學習框架 (CPU 版本)
paddlepaddle==3.0.0b2

# PaddleOCR 文字識別引擎
paddleocr==3.3.1

# ============================================
# 圖像處理依賴
# ============================================
# OpenCV (PaddleOCR 需要)
opencv-python==4.10.0.84

# Pillow 圖像庫
pillow==10.4.0

# NumPy (必須使用與 PaddleOCR 兼容的版本)
numpy==1.26.4

# ============================================
# 測試框架依賴
# ============================================
# pytest 測試框架
pytest==8.3.3
pytest-asyncio==0.24.0

# ============================================
# 其他必要依賴
# ============================================
# Python-dotenv (載入 .env 檔案)
python-dotenv==1.0.1

# Shapely (PaddleOCR 用於處理文字框座標)
shapely==2.0.6

# PyYAML (PaddleOCR 配置檔案)
pyyaml==6.0.2

# ImgHash (PaddleOCR 可能需要)
imagehash==4.3.1

# LMDB (PaddleOCR 某些功能可能需要)
lmdb==1.5.1

# tqdm (進度條，PaddleOCR 使用)
tqdm==4.66.5

# Requests (下載模型)
requests==2.32.3

# ============================================
# 版本說明
# ============================================
# numpy 1.26.4: 最新的與 PaddleOCR 兼容的版本
# opencv-python 4.10.0.84: 與 numpy 1.26.4 兼容
# paddlepaddle 3.0.0b2: 與 PaddleOCR 3.3.1 兼容的最新版本
# paddleocr 3.3.1: 支援繁體中文的最新穩定版
